{
  "mcpServers": {
    "foundry": {
      "command": "http",
      "url": "http://localhost:4000/foundry/mcp",
      "auth": {
        "type": "api_key",
        "env": "FOUNDRY_MCP_API_KEY",
        "key": "Bearer ${FOUNDRY_MCP_API_KEY}"
      },
      "tools": [
        "project_status",
        "module_context",
        "system_graph",
        "submit_proposal",
        "proposal_status",
        "run_lint",
        "read_doc"
      ],
      "description": "Foundry MCP server — exposes project context, lint, proposal, and spec-kit document tools for AI agents."
    }
  },
  "_comment": "Generated by Foundry. Set FOUNDRY_MCP_API_KEY in your environment to enable auth. Point Claude Code, Cursor, or Codex CLI at this config file."
}
