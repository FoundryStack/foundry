{
  "mcpServers": {
    "foundry": {
      "type": "http",
      "url": "http://localhost:4000/foundry/mcp",
      "headers": {
        "Authorization": "Bearer ${FOUNDRY_MCP_API_KEY}"
      }
    }
  }
}
