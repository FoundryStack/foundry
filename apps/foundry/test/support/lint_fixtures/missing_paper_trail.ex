defmodule Foundry.LintFixtures.MissingPaperTrail do
  @moduledoc "Test fixture: Ash resource without AshPaperTrail"

  # Minimal stub — not a real Ash resource, just marked as one
  # for testing purposes
end
