defmodule Foundry.LintFixtures.MissingArchival do
  @moduledoc "Test fixture: Ash resource without AshArchival"

  # Minimal stub — not a real Ash resource, just marked as one
  # for testing purposes
end
